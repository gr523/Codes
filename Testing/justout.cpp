#include <bits/stdc++.h>
#define ll long long 
using namespace std;

int main(){
    FILE *fp;

    if(!(fp=fopen("file","r"))){
        cout<<"problem"<<"\n";
        return 0;
    }
    cout<<"NO PROBLEMO"<<"\n";
    fclose(fp);
}